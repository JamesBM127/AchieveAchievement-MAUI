
using AchieveAchievement.ViewModel;

namespace AchieveAchievement.View.SettingsView;

public partial class ContactSettingsView : ContentView
{
    public ContactSettingsView()
    {
        InitializeComponent();
    }

    private void AddContactSettingsBtn(object sender, EventArgs e)
    {

    }

    private void EditBtnClicked(object sender, EventArgs e)
    {
        //ImageButton clickedButton = sender as ImageButton;
        //Grid gridParent = clickedButton.Parent as Grid;
        //Grid gridGrandParent = gridParent.Parent as Grid;
        //Entry entryEncontrado = null;

        //foreach (var child in gridGrandParent.Children)
        //{
        //    if (child is Frame frame)
        //    {
                
        //        entryEncontrado = (Entry)entry.FindByName("EntryLinkSettings");
        //        break; // Saia do loop assim que encontrar o Entry desejado
        //    }
        //}
    }
}